import { Component, inject } from '@angular/core';import { AsyncPipe } from '@angular/common';import { combineLatest, map } from 'rxjs';import { DataService } from '../../core/services/data.service';import { standings } from '../../core/utils/scoring';
@Component({standalone:true,imports:[AsyncPipe],templateUrl:'./leaderboard.component.html',styleUrl:'../shared-page.scss'})export class LeaderboardComponent{data=inject(DataService);vm$=combineLatest([this.data.users$(),this.data.fixtures$(),this.data.lineups$(),this.data.events$()]).pipe(map(([users,fixtures,lineups,events])=>{const s=standings(users,fixtures,lineups,events);return{fantasy:s,league:[...s].sort((a,b)=>b.leaguePoints-a.leaguePoints||b.goalsFor-b.goalsAgainst-(a.goalsFor-a.goalsAgainst))}}));}
