import { Component, inject, signal } from '@angular/core';import { AsyncPipe, DatePipe } from '@angular/common';import { FormsModule } from '@angular/forms';import { DataService } from '../../core/services/data.service';import { BONUS_RULES } from '../../core/constants/bonus-rules';import { Fixture } from '../../core/models';import { ToastService } from '../../core/services/toast.service';
@Component({standalone:true,imports:[AsyncPipe,DatePipe,FormsModule],templateUrl:'./admin.component.html',styleUrl:'../shared-page.scss'})export class AdminComponent{data=inject(DataService);toast=inject(ToastService);rules=BONUS_RULES;fixtures$=this.data.fixtures$();selFixture?:Fixture;selTeam='';ruleIds:string[]=[];home=0;away=0;saving=signal(false);teams(f:Fixture){return[f.homeTeam,f.awayTeam]}toggle(id:string){this.ruleIds=this.ruleIds.includes(id)?this.ruleIds.filter(x=>x!==id):[...this.ruleIds,id]}async import(){const n=await this.data.importSeedFixtures();this.toast.show(`${n} partite importate`)}async addEvents(){if(!this.selFixture||!this.selTeam||!this.ruleIds.length){this.toast.show('Seleziona partita, squadra e bonus/malus','error');return}await this.data.addTeamEvents(this.selFixture,this.selTeam,this.ruleIds);this.toast.show('Bonus/Malus inseriti');this.ruleIds=[]}async result(f:Fixture){await this.data.setResult(f,this.home,this.away);this.toast.show('Risultato salvato')}}
